# Snake (ncurses)

Simple Snake en C avec interface ncurses.

## Build (CMake)

```bash
mkdir -p build
cmake -S . -B build
cmake --build build
```

## Lancer

```bash
./build/snake
```

## Controles

- Fleches ou WASD pour bouger
- `q` pour quitter
